# VANT-Agent Configuration
# Copy to config.yaml and customize for your environment

server:
  url: "http://VANT-SIEM-SERVER:8003"
  logs_url: "http://VANT-SIEM-SERVER:9201"
  auth_mode: "none"  # none | basic | token
  auth_token: ""
  auth_username: ""
  auth_password: ""
  timeout: 15
  tls:
    verify: true
    ca_cert: ""

output:
  endpoint: ""
  source_endpoint: ""
  timeout_seconds: 10
  auth:
    mode: "none"
    username: ""
    password: ""
    token: ""
  tls:
    enabled: false
    verify: true
    ca_cert: ""

control:
  server_url: ""
  require_https: false
  token: ""
  poll_seconds: 30
  verify_ssl: true
  inventory_seconds: 86400
  dlp_poll_seconds: 60
  dlp_scan_seconds: 30

agent:
  id: ""  # auto-generated if empty
  host_name: ""  # auto-detected if empty
  check_interval: 60        # seconds between log collection cycles
  heartbeat_interval: 300   # seconds between heartbeats
  max_batch_size: 500
  log_every_cycles: 60

collectors:
  snort:
    enabled: false
    path: "/var/log/snort/alert_json.txt"
    source_type: "snort"
  suricata:
    enabled: false
    path: "/var/log/suricata/eve.json"
    source_type: "suricata"
  windows_eventlog:
    enabled: false
    channels:
      - Security
      - System
      - Application
  postgres:
    enabled: false
    path: "/var/log/postgresql/postgresql-*.log"
  file_logs:
    enabled: false
    items: []

inventory:
  enabled: true
  interval: 300  # seconds between inventory submissions (default 5min)

dlp:
  enabled: false
  scan_paths:
    - "C:\\Users"
    - "C:\\Documents"
  scan_extensions:
    - ".docx"
    - ".xlsx"
    - ".pdf"
    - ".txt"
  keywords:
    - "clasificado"
    - "secreto"
    - "restringido"

logging:
  level: "INFO"
  file: ""  # auto: executable_dir/vant-agent.log
  max_bytes: 10485760   # 10MB
  backup_count: 5
