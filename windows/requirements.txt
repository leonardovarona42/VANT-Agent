# VANT-Agent Windows - Dependencias
# Para compilar offline, usa: pip install --no-index --find-links=../vendor -r requirements.txt

PyYAML>=6.0
requests>=2.28.0
Pillow>=10.0.0
pypdf>=4.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
python-pptx>=0.6.0
odfpy>=1.4.0
olefile>=0.47

# PyQt6 (opcional, para bandeja del sistema)
# PyQt6>=6.10; platform_system == "Windows"

# PyInstaller (opcional, para compilar a .exe)
# pyinstaller>=5.0
